apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard-react
  namespace: platform-services
spec:
  selector:
    app: admin-dashboard-react
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
